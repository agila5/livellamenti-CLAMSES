# 1. Supponendo che un cerchio abbia raggio r = 4, come fareste a calcolarne il
# perimetro? E l'area?

# NB: La costante pi-greco e' implementata nel vettore pi
pi

# 2. Dopo aver letto la pagina di help delle funzioni ?choose e ?factorial,
# provate a calcolare:

# - 10!
# - coefficiente binomiale (10 3)

# 3. Si definisca un vettore x che contiene gli esiti ottenuti per n = 6 esami
# diversi (scegliete pure voi i voti).

# - Come si calcola il voto medio?
# - La deviazione standard dei voti?
# - E se volessimo calcolarne la varianza?

# - NB: La formula utilizzata da R per calcolare la varianza di un vettore di n 
#   elementi prevede di dividere la somma degli scarti dalla media al quadrato 
#   per (n - 1). Come fareste per correggere il risultato restituito da R ai 
#   punti precedenti ottenendo la varianza come somma degli scarti dalla media 
#   al quadrato diviso n invece che n - 1?

# 4. Si utilizzi l'help di R per cercare quali funzioni possono essere usate per
# ordinare un vettore numerico di n elementi. Successivamente, si ordini il
# vettore
x <- c(10, 4, 2, -3, 11, -5)
# elencando gli elementi in ordine decrescente.
